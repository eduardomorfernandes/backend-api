
# Simple Backend API (FastAPI)

A minimal FastAPI backend showing clean structure and production-friendly design.

## Run locally
```
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## Routes
- `/health`
- `/greet/{name}`
